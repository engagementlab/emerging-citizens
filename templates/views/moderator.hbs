{{> moderator/header_htyi }}

<div class="page-container">
    <div class="row">
        <div class="col-sm-12">                
            {{!-- <select id="game_type" name="gameType">
            {{#each gameTypes}}
              <option value="{{@index}}">{{this}}</option>
            {{/each}}
            </select><br /> --}}

            <div class="col-sm-3"></div>
            <div id="tweet-submission" class="col-sm-6 input-box">
              <div class="form-prompt white">de-select any content to not include in this session:</div>
              <div class="form">
                    {{#each categories}}
                        <button value="{{id}}" class="voting-button active submit">{{name}}</button>
                    {{/each}}                
                </div>
            
                <div class="form-prompt white">access code:</div>
                <div id="access-code">{{gameCode}}</div>

                <input id="btn_submit" data-event="game:start" class="button_text" type="submit" name="submit" value="Start!">
            </div>

        </div>
    </div>
</div>